<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.a79069.homeworkmvp">

    <application
        android:name="com.example.a79069.homeworkmvp.app.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".loginTask.LoginTaskActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".studentMainTask.StudentTaskActivity"/>
        <activity android:name=".registerTask.RegisterActivity"/>
        <activity android:name=".forgetTask.ForgetActivity"/>
    </application>

</manifest>
